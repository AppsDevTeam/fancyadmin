{extends ADT\Forms\BaseForm::getDefaultTemplateFile()}

{define row}
	<div {if $section['name']}id="{$section['name']}"{/if} class="row">
		<div n:foreach="$section['children'] as $_el" class="col-md-{12 / count($section['children'])}">
			{include renderEl, el => $_el}
		</div>
	</div>
{/define}